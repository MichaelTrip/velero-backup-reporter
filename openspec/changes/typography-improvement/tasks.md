## 1. Font Dependencies

- [x] 1.1 Install `@fontsource-variable/inter` and `@fontsource-variable/jetbrains-mono` npm packages

## 2. Font Integration

- [x] 2.1 Import Inter and JetBrains Mono CSS in `main.js`
- [x] 2.2 Override `--p-font-family` on `:root` in `App.vue` to use Inter; define `--font-mono` custom property for JetBrains Mono

## 3. Typography Tuning

- [x] 3.1 Add global typography styles in `App.vue`: base font size, line height, and font smoothing for crisp rendering
- [x] 3.2 Apply `--font-mono` to log output `<pre>` blocks in `BackupDetailView.vue`
- [x] 3.3 Apply `--font-mono` to label/annotation Tag badges in `BackupDetailView.vue`

## 4. Build and Verify

- [x] 4.1 Build the frontend and Go binary, run tests
